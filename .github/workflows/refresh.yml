name: Refresh dashboard data

on:
    schedule:
    # Every 30 min, ~9am–8pm IST  ->  03:00–14:30 UTC (IST = UTC+5:30)
    - cron: "0,30 3-14 * * *"
  workflow_dispatch: {}

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: refresh-data
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Incremental sync from TATA
        env:
          TATA_TOKEN: ${{ secrets.TATA_TOKEN }}
        run: python scripts/sync.py

      - name: Sync index.html to dashboard
        run: cp inbound-dashboard.html index.html

      - name: Commit if data changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- inbound-dashboard-data.js inbound_call_records.csv sync_state.json index.html; then
            echo "No data change — nothing to commit."
          else
            git add inbound-dashboard-data.js inbound_call_records.csv sync_state.json index.html
            git commit -m "Auto-refresh data ($(date -u '+%Y-%m-%d %H:%M UTC'))"
            for i in 1 2 3; do
              git push && break
              echo "Push rejected — pulling latest and retrying ($i)..."
              git pull --rebase --autostash origin main
            done
          fi

      - name: Business-health alert (emails only if something is wrong)
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python scripts/alert.py

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Notify on sync failure
        if: failure()
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: |
          python - <<'PY'
          import os, smtplib
          from email.mime.text import MIMEText
          u=os.environ["GMAIL_USER"]; p=os.environ["GMAIL_APP_PASSWORD"]
          to="rohit.bagga@myhq.in"
          m=MIMEText("The hourly data refresh workflow FAILED.\n"
                     "Check: https://github.com/myHQBuddy/inbound-dashboard/actions","plain","utf-8")
          m["Subject"]="🔴 Inbound sync FAILED"; m["From"]=u; m["To"]=to
          with smtplib.SMTP_SSL("smtp.gmail.com",465) as s:
              s.login(u,p); s.sendmail(u,[to],m.as_string())
          print("failure email sent")
          PY
